from typing import Sequence

class FragmentChecker:
    """
        Check allowed fragments for template
    """
    def check(self, template_name: str, fragment_names: Sequence[str]) -> bool:
        pass

